import { RouterSingleton } from 'simple-api-ts';

export default class Router extends RouterSingleton {
  public createRoutes(): void {
    // const routes = RouterSingleton.getInstance().getRoutes();
  }
}
